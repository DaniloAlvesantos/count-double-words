# Count Double Words
It's a simple CLI library for counting double words with node.js

## How to use it
You need to pass the input file using the flag _-i_ and the path from it. And for output you can use the flag _-o_ and the path.

```Bash
node src/cli.js -i input.txt -o ./results
```

